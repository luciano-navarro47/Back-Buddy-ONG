import { Router } from "express";
// import { a } from "a";

const processPaymentRouter = Router();

processPaymentRouter.get("/", );
processPaymentRouter.post("/", );
processPaymentRouter.put("/:id", );
processPaymentRouter.delete("/:id", );

export default processPaymentRouter;
